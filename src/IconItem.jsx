import React from 'react';

const IconItem = ({ text }) => (
  <li className="attached auto-property-bundle">
    <span>
      <span>&nbsp;</span>
      <small></small>
      <a href="#">{text}</a>
    </span>
  </li>
);

export default IconItem;
